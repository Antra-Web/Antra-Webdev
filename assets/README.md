This folder is populated automatically by the GitHub Actions workflow
(.github/workflows/portfolio-screenshots.yml), which runs
scripts/capture-screenshots.mjs.

Do not hand-edit files here except for a one-time manual seed if you want
thumbnails to appear before the workflow's first run — after that, this
directory (including manifest.json) is fully managed by the workflow.
